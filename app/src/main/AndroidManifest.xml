<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.agh.edu.pankracy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CalendarActivity"></activity>
        <activity
            android:name=".WeatherTestActivity"
            android:label="@string/title_activity_weather_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PlantFormActivity" />
        <activity android:name=".PlantDetailsActivity" />
        <activity android:name=".PlantListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".utils.BootCompletedDetection"
            android:label="BootCompletedDetection">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <provider
            android:name=".data.PlantProvider"
            android:authorities="com.agh.edu.pankracy.plants"
            android:exported="false" />

        <service android:name=".notifications.NotificationIntentService"
            android:exported="false"/>
    </application>

</manifest>